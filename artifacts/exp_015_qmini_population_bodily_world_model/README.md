# exp_015 artifacts

This directory is reserved for immutable source-audit, Isaac Lab import, and
later measured transition artifacts. No teacher, PPO, world-model, MoE, GA,
QD, or deployment artifact is stored here.

The current Stage 1 run has no formal baseline trajectory data. The official
Qmini URDF/STL bytes are vendored under the experiment asset directory and
their source hashes are recorded in the manifests.
