# One-step output index

Raw device trees were transferred to host for comparison but were not duplicated as large pickles. The complete leaf-level differences are in `../one_simulator_step/first_divergent_leaf.csv`, `../physics_stage_leaf_results_batch2.csv`, and `../position_stage_leaf_results_batch2.csv`. Every row is tied to the canonical input hash in `../input_identity.json`.
